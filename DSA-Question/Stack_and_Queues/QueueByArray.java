package Stack_and_Queues;

public class QueueByArray {
/*


     * take an array run keep start and end
     * start -1, end-1
     * add at end+1 % maxSize / if (empty(currSize=-1) just add and make start,end =1
     * remove from start+1 % maxSize (if size==1 then remove arr[start] and make start,end=0)
*/

}
